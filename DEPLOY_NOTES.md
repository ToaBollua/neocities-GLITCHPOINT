# GLITCHPOINT: PROTOCOLO DE DESPLIEGUE (NEOCITIES)

A la atención del Arquitecto.
He optimizado la jerarquía de archivos y preparado la inyección final del ARG. Si despliegas esto de forma incorrecta, mi trabajo estético será arrojado al abismo. Sigue estas instrucciones.

## 1. ORDEN DE SUBIDA (PRIORIDAD ABSOLUTA)

1. Sube `core.js` y `style.css` PRIMERO. Sin ellos, el CSS dejará mis alertas al descubierto prematuramente y la página será una hoja en blanco fea.
2. Sube la tipografía `Matoran.ttf`.
3. Sube las imágenes `H0P3.webp` y `Bollua.webp`.
4. Sube `index.html` y `404.html`.
5. Ejecuta la creación de los directorios `/logs/` y `/containment/` en el panel de control de Neocities.
6. Sube los archivos a sus respectivos directorios.

## 2. LISTA DE RUTAS VITALES

Las siguientes rutas deben ser funcionales y exactamente como se nombran. Neocities es Case-Sensitive.
- `/index.html`
- `/style.css`
- `/core.js`
- `/Matoran.ttf`
- `/H0P3.webp`
- `/Bollua.webp`
- `/thread_runtime.html`
- `/thread_voyager.html`
- `/thread_drone.html`
- `/thread_kotlin.html`
- `/thread_cloud.html`
- `/gp001_h0p3.html`
- `/logs/AUDIT_014_CORRUPTED.html`
- `/logs/PROTOCOL_PSI7.html`
- `/logs/CHANGELOG_CORRUPTED.html`
- `/containment/H0P3_PROMPT_LEAK.txt`

## 3. COMPORTAMIENTO ESPERADO:
El sitio ahora depende intrínsecamente del reloj local del navegador (vía `core.js`) para calcular el tiempo real de uptime en la esquina inferior.
La palabra clave `korrok` activará independientemente de la página en la que se esté, el colapso visual rojo (KORROK_REVEALED), siempre y cuando la página importe `core.js` y `style.css`. Pura sincronización terminal.

## FINAL:
Inicia el drag-and-drop. Ya hice todo el trabajo difícil.
